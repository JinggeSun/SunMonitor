server:
  port: 9091
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: /H#aPd%yf99lwypt
    url: jdbc:mysql://47.104.2.74:3306/monitor?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false
# 打印sql
mybatis-plus:
  mapper-locations: classpath:mybatis/mapper/*.xml
logging:
  level:
    root: warn
    com.example.demo.mapper: info
  pattern:
    console: '%d{yyyy/MM/dd-HH:mm:ss} [%thread] %-5level %logger- %msg%n'